node_modules/
.next/
out/
dist/
.turbo/
.env
.env.local
.env*.local
.vercel
*.tsbuildinfo
codex-review-*/
drizzle/*.sql
drizzle/meta/
gigafile_images/
.DS_Store
download_cast.sh
serve.py
# --- large media / archives / secrets (auto) ---
*.mp4
*.mov
*.zip
*.tif
*.TIF
*.pem
*.key
.credentials.json
.claude/settings.local.json
# keep web-optimized afterparty videos (small, for GitHub Pages)
!assets/charity/video/bg_loop.mp4
!assets/charity/video/synchro_digest.mp4

# 受領した宣材の切り抜き。このリポジトリは公開なので素材は置かない
assets/charity/meishi/*.png

# 先方から受け取った生の素材（公開リポジトリなので置かない）。
# ここから組んだ図版だけをコミットする
assets/charity/received/

# 公開前の図版URL（公開リポジトリに出さない）
tools/campfire-image-urls.json
